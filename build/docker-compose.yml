version: '3'

# for build only
services:
  magicmirror:
    build: 
      context: ./
      args: 
        myimage: "node:slim"
        MagicMirror_Version: ${MagicMirror_Version}
    container_name: mm
    image: karsten13/magicmirror:debian_${MagicMirror_Version}