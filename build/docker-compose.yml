version: '3'

# for build only
services:
  magicmirror:
    build: 
      context: ./
      args: 
        myimage: "arm32v7/node:slim"
        MagicMirror_Version: ${MagicMirror_Version}
    container_name: mm
    image: karsten13/magicmirror:rpi_${MagicMirror_Version}