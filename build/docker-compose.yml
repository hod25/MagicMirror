version: '3'

# for build only
services:
  magicmirror:
    build: 
      context: ./
      args: 
        myimage: "debian:jessie"
        MagicMirror_Version: ${MagicMirror_Version}
    container_name: mm
    image: karsten13/magicmirror:debian