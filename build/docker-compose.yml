version: '3'

# for build only
services:
  magicmirror:
    build: 
      context: ./
      args: 
        myimage: "resin/armv7hf-node:9-slim"
        MagicMirror_Version: ${MagicMirror_Version}
    container_name: mm
    image: karsten13/magicmirror:rpi_${MagicMirror_Version}